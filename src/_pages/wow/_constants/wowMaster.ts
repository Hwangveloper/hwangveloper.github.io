export enum EWowMasterCategory {
  KEYSTONE = 'KEYSTONE',
}

export enum EWowMasterSubcategory {
  EMPTY = '',
  EVERY_DUNGEON = 'EVERY_DUNGEON',
}

export enum EWowMasterResetType {
  week = 'week',
  month = 'month',
  manual = 'manual',
}